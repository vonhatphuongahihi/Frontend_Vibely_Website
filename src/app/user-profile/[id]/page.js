//import React from "react";

import ProfileHeader from "../ProfileHeader";
import ProfileTabs from "../ProfileTabs";

const Page = () => {
    return (
        <div>
        <ProfileHeader />
        <ProfileTabs />
        </div>
    );
};

export default Page;